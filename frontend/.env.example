# Leave empty to use Vite dev proxy (/api -> http://localhost:8042) in local dev.
# For an Nginx deployment that proxies /api to the backend, set this to /api before build.
# Or set it to a fully qualified backend URL, e.g. http://43.199.73.102:8042
VITE_API_BASE_URL=
